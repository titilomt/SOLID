{
  "compilerOptions": {
    "types": ["jest", "node"],
    "target": "es6",                           // Define a versão do ECMAScript de saída.
    "module": "ES2022",                      // Define o sistema de módulos.
    "outDir": "./dist",                        // Diretório para os arquivos de saída.
    "strict": true,                            // Habilita uma ampla gama de verificações de tipo.
    "rootDirs": ["src", "__tests__"],          // Lista de diretórios raiz que contêm arquivos de entrada.
    "esModuleInterop": true,                   // Permite a interoperabilidade de módulos ES6 com CommonJS/AMD/UMD.
    "skipLibCheck": true,                      // Pula a verificação de tipos nos arquivos de declaração.
    "forceConsistentCasingInFileNames": true   // Garante que o caso dos arquivos importados corresponda ao caso dos arquivos reais.
  },
  "include": ["src/**/*", "__tests__/**/*"],                     // Arquivos a serem incluídos na compilação.
  "exclude": ["node_modules"]  // Arquivos/pastas a serem excluídos da compilação.
}